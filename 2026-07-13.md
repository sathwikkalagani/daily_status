# 2026-07-13

## Today's Goal

* Master Bitwise Operators and their applications

## Completed

* ✅ Binary Number System
* ✅ Bit Positions (MSB & LSB)
* ✅ Left Shift (`<<`)
* ✅ Right Shift (`>>`)
* ✅ Bit Masking
* ✅ AND (`&`)
* ✅ OR (`|`)
* ✅ XOR (`^`)
* ✅ NOT (`~`)
* ✅ Creating masks using `1 << n`

### Programs Practiced

* ✅ Check the nth bit
* ✅ Set the nth bit
* ✅ Clear the nth bit
* ✅ Toggle the nth bit
* ✅ Extract multiple bits from a number/register
* ✅ Manual binary conversions
* ✅ Manual bitwise calculations using AND, OR, XOR, NOT, and Shift operators
* ✅ Solved multiple practice problems without using a compiler

## Time Spent

* Approximately 6–7 hours

## Problems Faced

* Initially confused about bit masking.
* Took time to understand why `1 << n` creates a mask.
* Understood why `&`, `|`, `^`, and `~` are used for different bit operations instead of memorizing the formulas.

## What I Learned

* Bitwise operators work at the individual bit level.
* `1 << n` creates a mask with only the nth bit set.
* `&` is used to check bits.
* `|` is used to set bits.
* `&` with `~` is used to clear bits.
* `^` is used to toggle bits.
* How to extract a group of bits using shifting and masking.
* Understood the logic behind every operation by performing binary calculations manually.

## Tomorrow's Goal

* Complete the remaining Preprocessor Directives.

